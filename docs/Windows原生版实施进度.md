# Windows 原生版实施进度

2026-09-06。用户已确认需求、架构、交互和实施计划，当前处于连续开发阶段。

## 当前可运行版本

0.2.0 开发预览版已实现型号与设备建档、编辑、分页查询、停用、重复校验、并发版本校验、操作回执和 SQLite 持久化。原生 WPF 主窗口与编辑窗口支持未保存提示和窗口位置恢复。

状态记录与异常登记/关闭已接入原生窗口：五种状态、历史读数前后校验、来源展示、分页、未保存输入保护与重启保留均有实际验证。故障维修、库存、导入、统计与备份尚不属于此安装包的已完成功能。T4 故障维修正在实施。不得据此生成完整软件验收结论。

## 已取得证据

- Windows 11 x64 本机、.NET SDK 10.0.400；Release 构建通过，0 警告、0 错误。
- T3 数据层 28 项测试通过，覆盖重复命令、冲突、分页、回滚、迁移拒绝及安全关闭。
- 锁定依赖恢复通过；NuGet 已知漏洞扫描无发现。
- 原生 UI Automation 实际运行：型号建档、设备建档、无匹配/匹配查询、重复校验、未保存放弃、退出与重开后数据保留。
- 自包含 x64 安装包成功生成；本机中文路径安装、安装后 UI 测试、卸载及隔离测试数据库哈希保留通过。
- C# 存储审查和桌面/脚本审查已执行；已修复初始化提前修改不兼容库、异常连接释放、关闭排空和查询测试误判。

原始测试 TRX、JSON、日志、截图和 EXE 保留于本地 artifacts 目录，不纳入源码提交。

## 尚待验证

干净 Windows 机器、标准用户权限环境、快捷方式、跨 Windows 会话唤醒、多显示器与高 DPI、默认用户目录卸载保留数据均须进一步验证。当前安装向导为英文，应用界面为中文；安装包尚未签名。测试结果仅覆盖已描述路径。

## 继续顺序

继续 T4 故障维修闭环、库存、CSV/模拟、统计、备份恢复及整体验收。软著材料须在功能冻结后以同版本源码和真实窗口重新生成，正式申请字段与材料审核仍保留人工门禁。

T3 验证补充：0.2.0 中文路径安装、状态补录拒绝倒退、异常关闭与重启查看、未保存关闭取消、卸载保留隔离数据库均通过。源码以分阶段提交对应版本；0.1.0 历史提交 ca76a7d 已推送。
